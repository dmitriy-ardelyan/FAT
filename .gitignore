#here we ignore out folder we do not need these class files compiled during test builds.
out/
#to ignore some file types...
filetypesIgnoreExampleFolder/*.txt
#to gignore exact file
.idea/workspace.xml